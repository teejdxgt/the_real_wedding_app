<%= semantic_form_for(@micropost) do |f| %>
    <%= f.input :content, :label => "" %>
	<%= f.buttons do %>
	  <%= f.commit_button :label => "Post", :button_html => { :class => "post" } %>
    <% end %>
<% end %>
